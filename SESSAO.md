# Brief — Granativa (ex-Zerou)

## Estado atual

SaaS/PWA financeiro mobile-first (React 19 + Firebase Firestore + Vercel). Duas frentes: controle individual e organização a dois (casal). App em **lançamento gratuito** — sem cobrança, checkout ou página de planos ativa. O projeto Firebase está no **Blaze**, mas o produto segue gratuito e sem Cloud Functions no fluxo principal. Produção: https://granativa.com.br (domínio próprio comprado no registro.br, DNS configurado no Vercel — `zerou-five.vercel.app` continua ativa como URL legada). Trabalho direto na `main`.

**Rebrand completo**: app renomeado de "Zerou" → "Granix" → **Granativa** (grana + ativa). Logo: duas bolas sobrepostas (sólida laranja + outline escuro). Assets em `public/brand/granativa-*.png` — todos com transparência real (alpha channel). Logo horizontal usada no nav/footer/og:image (`granativa-logo-horizontal.png`); logo empilhada em auth/onboarding (`granativa-logo-primary.png`). Favicons: `favicon-16x16.png` e `favicon-32x32.png`. Version strings internas (`zerou-v12.2-*`, `zerou-cache`) e localStorage keys (`zerou.themeMode`, `zerou.themeId`) mantidas por retrocompatibilidade.

A interface segue a direção visual **"Sol"** (areia quente clara + tangerina `#EE5524`, números em DM Sans 800, corpo em Instrument Sans) e é **mobile-nativa**: nav inferior com FAB central (slot 2 = Extrato/Transações, slot 4 = Cartões, Casal no menu Mais + indicador de ponto ativo), telas de lançamento com header de valor gigante colorido por tipo, seletores em **bottom-sheet** (conta, categoria, bandeira), categorias com ícone e cor editáveis, onboarding em questionário com barra de progresso, e empty states ilustrados. Detalhes em `docs/design/DESIGN.md`.

Primeiro acesso (2026-07-12): depois do questionário de onboarding, um **tour de boas-vindas em 6 slides** (`WelcomeTour` + `welcomeTour.store` em `src/onboarding/`, montado no `AppShell`) abre sozinho uma vez apresentando os pilares do app. "Já viu" no localStorage (`zerou.welcomeTourSeen`), reabrível em "Como funciona" (menu Mais/sidebar). Sequenciado **antes** do mini-tutorial do "Disponível" (`AvailableModeSheet` só auto-abre quando `useWelcomeTour().seen`), pra não empilhar dois modais.

Todas as páginas autenticadas têm agora **cabeçalho compacto** (eyebrow + título, sem parágrafo), **ícones de categoria** em tile colorido 36×36 (`CategoryMark`) nas listas de transações (Dashboard e TransactionsPage), **cards de conta** com gradiente escuro (`--gradient-slate`) e **formulários de cadastro colapsáveis** (Contas, Cartões, Compromissos).

A aba **Análise** (`/app/search`) exibe: KPI strip (gasto total, maior categoria, variação % vs. mês anterior com ícone trending); donut 200px interativo (Recharts — clique destaca fatia, centro mostra nome/valor/%) com legenda de **barras de progresso** por categoria; seção **"Compras parceladas — Em andamento"** (valor cheio da compra + quanto falta); gráfico de barras (220px) entradas vs saídas dos últimos 6 meses com legenda própria; busca por texto (card oculto quando vazio). O gasto é contado em **regime de caixa (por parcela)** — cartão entra pela parcela da fatura de cada mês, não pela compra cheia no mês da compra (`src/finance/spendingAnalysis.ts`). Dá pra **avançar pra meses futuros**: mês futuro mostra "Previsto" — comprometido (parcelas de cartão + contas a pagar) + recorrências projetadas (estimativa), não gasto realizado.

**Cloud Functions — dois codebases** (`firebase.json`, `southamerica-east1`): `functions/` (codebase `billing`) tem as 4 funções scheduled — `closeInvoicesDue` (meia-noite), `generateRecurrences` (6h), `sendDueReminders` (8h), `sendDailyLogReminder` (20h — lembrete diário via FCM) — mais o scaffold Stripe (inativo). `functions-admin/` (codebase `admin`) é **isolado de propósito** (desde 17/06) e tem só `adminDeleteUser` — sem depender de secrets do Stripe, deploya independente. **`adminDeleteUser` só existe em `functions-admin/src/index.ts`** — nunca recriar em `functions/src/`, isso já causou um conflito real de deploy ("More than one codebase claims...") em 07/07. Deploy: `npx firebase deploy --only functions --project zerou-26757` (deploya os dois codebases). Push via `sendPushToUser` (`push.ts`) com limpeza automática de tokens stale. VAPID key configurada no Vercel. Secrets Stripe como placeholder para deploy sem ativar billing.

O app autenticado não usa mais logo persistente no topo; o onboarding também fica sem bloco de marca para preservar altura útil no celular.

A landing pública (`/`) foi reescrita com Framer Motion: **hero claro** (branco → areia) com grade perspectiva CSS, stage (phone + badges flutuantes) que inclina em 3D com o mouse (`rotateX/Y` + `preserve-3d`, badges em Z-depths distintos), gloss de luz na tela do phone (`useMotionTemplate`), scroll parallax e stagger de entrada. Seções abaixo: stats band, bento com `TiltCard`, modo casal, steps, FAQ e CTA dark. Sempre clara (Paper), nunca dark. **Mobile-first**: hover effects (`whileHover`, estilos `:hover`) ativados apenas em `@media (hover: hover)` / `window.matchMedia` — sem efeito em touch. Stats band horizontal em todos os tamanhos (sem empilhar), botões hero full-width em `≤640px`, ghost "Entrar" oculto em `≤480px`.

## Leitura inicial

1. Leia este arquivo.
2. Use `docs/BUSCA_RAPIDA.md` para localizar contexto específico.
3. Não abra histórico mensal ou docs grandes sem uma busca `rg`/Grep antes.

## Stack

React 19 (TS strict), Vite, Firebase Web SDK (Auth + Firestore + Storage), Vercel, Vite PWA, React Router, React Hook Form, Zod, Zustand, Lucide React, **Recharts** (gráficos). Node >= 22.

## Onde procurar

| Assunto | Arquivo |
|---|---|
| Mapa geral | `docs/BUSCA_RAPIDA.md` |
| Histórico por mês | `docs/history/YYYY-MM.md` |
| Design/UI (Sol) | `docs/design/DESIGN.md` |
| Pendências | `docs/planning/TODOS.md` |
| Testes/QA | `docs/qa/TESTES.md` |
| Arquitetura | `docs/ARCHITECTURE.md` |
| Segurança/privacidade/operação | `docs/SECURITY.md`, `docs/PRIVACY.md`, `docs/RUNBOOK.md` |

## Convenções essenciais

- Dinheiro em centavos inteiros (`amountCents`), exibido por `formatMoney()`.
- Firestore (não RTDB). IDs client-side + `clientMutationId`.
- Fluxos client-side com Security Rules; sem Cloud Functions no fluxo principal, mesmo com o projeto Firebase no Blaze.
- **Offline-first**: Firestore com `persistentLocalCache` + `experimentalAutoDetectLongPolling` (`src/firebase/config.ts`). **Nunca bloquear a UI esperando ack do Firestore** — dispara a escrita (fire-and-forget + `.catch`) e deixa o `onSnapshot` refletir (badge pendente → sincronizado).
- **Boot em internet fraca**: `AuthContext` inicializa estado **sincronamente** do `localStorage` — se há perfil em cache, `loading=false` já na primeira renderização e o app abre instantaneamente. Firebase confirma a sessão em background (timeout de fallback: 500ms). Enquanto `authFromCache` estiver ativo, ações sensíveis de login/verificação ficam bloqueadas até Firebase confirmar a sessão real. Google Fonts são carregados de forma não-bloqueante (`preload + onload`) e cacheados pelo Workbox com `CacheFirst`.
- **`useFinanceData` (2026-07-07)**: `loading` só vira `false` quando as 5 coleções (contas/categorias/transações/contas a pagar/recorrências) já entregaram o primeiro snapshot — não na primeira que chegar. Evita o Dashboard piscar "R$ 0,00" se `categories`/`bills` resolverem do cache antes de `accounts`. `DashboardPage` espera `finance.loading || cardsData.loading` para "Disponível"/"Comprometido" (dependem de faturas); "Saldo total" continua instantâneo, só depende de `finance.loading`.
- **Conta recém-criada**: `ensurePersonalFoundation` pode liberar a UI antes do ack do servidor; hooks financeiros devem tratar `permission-denied`/`unavailable` transitórios com retry curto antes de mostrar erro final.
- **Listeners protegidos**: para coleções por workspace/membership, preferir `subscribeWithTransientRetry` (`src/firebase/firestoreRetry.ts`) em vez de tratar `onSnapshot` error como falha final imediata.
- **Higiene de custo Firestore**: antes de gravar preferências, seeds ou metadados automáticos, compare se algo mudou e/ou memorize a preparação por sessão. Evite writes invisíveis em refresh/login/troca de rota; no Blaze isso aparece rapidamente na cota diária. Token FCM (`src/pwa/pushTokenCache.ts`) segue esse padrão: só grava no Firestore quando o token muda de fato, comparando com um cache local antes.
- **Quando adicionar `.limit()` numa coleção** (2026-07-07): o custo real de um `onSnapshot` é o tamanho da leitura inicial (1 leitura por doc que a query traz na montagem) — não "ter limite ou não". Regra prática: se uma coleção de um único workspace passar de ~500-1000 documentos, cada abertura de tela custa centenas de leituras; abaixo disso não compensa a complexidade. `accounts`/`categories`/`goals`/`recurring` nunca chegam lá (limitados pelo humano). `bills` cresce devagar. `sharedExpenseClaims`/`settlements` do casal são as que mais podem crescer com uso contínuo — reavaliar se algum workspace passar dessa marca. Sinal de alerta no painel do Firestore: leitura **por usuário ativo** subindo ao longo do tempo (não só leitura total crescendo com a base).
- **TTL nativo do Firestore**: `coupleInvites` tem uma política de TTL configurada no campo `expiresAt` (Google Cloud Console → Firestore → TTL policies) — convites expirados (48h) são apagados automaticamente pelo Firestore, sem Cloud Function. Não remover esse campo sem desligar a política primeiro.
- **Mobile iOS**: bottom sheets e formulários internos precisam de `min-width: 0`, contenção de `overflow-x` e controles segmentados fluidos; Safari pode permitir arrasto lateral em containers internos mesmo com `body` travado.
- **PWA offline**: SVGs também entram no precache do Workbox; logos de banco em `public/bank-logos/` devem funcionar offline após o service worker atualizar.
- **Prompt de instalação** (2026-07-08): `src/pwa/installPrompt.ts` captura `beforeinstallprompt` desde o boot (import de efeito colateral em `main.tsx`, singleton com listeners) — não montar a lógica de captura só dentro de um componente de página, o evento pode disparar antes da rota carregar. `InstallPromptSheet` (montado só em `DashboardPage`) mostra botão "Instalar agora" quando o navegador suporta o evento nativo (Android/Chrome/Edge/desktop), ou o tutorial de 3 passos quando `isIOSDevice()` for true (Safari não tem `beforeinstallprompt`). Nunca aparece se `isRunningStandalone()` (já instalado) ou se a pessoa já fechou o aviso (`zerou.pwaInstallDismissed` no localStorage, permanente).
- **Exclusão de conta** (`/app/settings/security/login-methods`, exige digitar `EXCLUIR`). **Reescrita em 2026-07-13** — a versão de 2026-07-08 apagava todos os dados do Firestore *antes* de tentar deletar o usuário do Firebase Auth, sem reautenticar; `deleteUser()` exige sessão recente e quase sempre falhava com `requires-recent-login` **depois** que os dados já tinham sumido, deixando a sessão viva — a pessoa caía em `/app/onboarding` como se fosse conta nova, sem logar de novo, e a conta nunca era excluída de fato (bug real, achado pelo dono, verificado ao vivo). `runAccountDeletion` (`accountDeletionService.ts`) corrige a ordem: reautentica (Google via popup, ou senha — pede o campo "Senha atual" pra quem não tem Google vinculado) **antes** de apagar qualquer dado; reautenticação falha → nada é tocado; exclusão do Auth falha mesmo assim (janela residual menor) → força `logout()` em vez de deixar sessão zumbi. Remove perfil, espaço pessoal, dados financeiros/cartões/faturas, billing shell e espaços de casal criados pelo usuário, e só depois deleta o usuário do Firebase Auth.
- **Tema local vs. Firestore** (2026-07-08): `appearance.store.ts` tem `hasLocalOverride` — depois da primeira troca manual de tema/densidade/fonte na sessão, `hydrateFromProfile` para de aplicar o que vem do Firestore (só serve pra hidratar o estado inicial). Sem isso, um snapshot do perfil em trânsito podia reverter uma escolha de tema recém-feita. Resetado no logout.
- **Tailwind v4 não processa `@tailwind base/components/utilities`** (2026-07-08): `global.css` ainda tem a sintaxe legada v3, mas o pacote instalado é v4 (`@tailwindcss/postcss`), que espera `@import "tailwindcss"`. Na prática isso significa que **o preflight nunca roda** — nenhum reset de `color`/`margin`/etc. do Tailwind chega a existir no CSS final. O sintoma real (2026-07-08): `<button>`/`<input>`/`<select>`/`<textarea>` sem classe própria caíam no preto padrão do navegador (`buttontext`), ilegível nos temas escuros — corrigido com um reset manual em `global.css` (`button, input, select, textarea { font: inherit; color: inherit; }`). Não migramos pro `@import` (mudança maior de pipeline); se aparecer outro sintoma de preflight faltando, o padrão é resolver com CSS próprio, não com hacks por componente.
- **Regras do Firestore para o membro do casal exigem `displayName`** (2026-07-08): `validCoupleOwnerMemberCreate`/`validCouplePartnerMemberCreate` (`firestore.rules`) têm `displayName` na lista `keys().hasOnly([...])` — faltava antes e bloqueava silenciosamente a criação do espaço/aceite de convite (`Missing or insufficient permissions.` sem detalhe de qual campo). Se algum dia o payload do documento `members/{uid}` mudar em `sharedService.ts`, atualizar as duas regras juntas ou a escrita é negada inteira (é um `writeBatch`, qualquer doc que falhar a regra derruba o batch todo).
- **Entitlement de billing na regra segue o mesmo default do cliente** (2026-07-08): `canCreateCoupleWorkspaceByEntitlement(uid)` em `firestore.rules` usa `.get(key, default)` pra tratar `billingAccounts/billing_{uid}` ausente ou sem o campo `entitlements.canCreateCoupleWorkspace` como **permitido** — espelha o default de `billingService.ts` (`subscribeBillingAccount`). Antes lia o campo bruto sem default e podia negar mesmo com o cliente achando que tinha liberado (billing nunca foi ativado pra ninguém, então isso só aparecia com um `billingAccounts` incompleto/de teste sobrando).
- **Mudar `firestore.rules` conta como billing pro classificador de segurança do Claude Code**: qualquer edição de regra que toque `billingAccounts`/`entitlements`, mesmo pra só destravar uma feature gratuita, é bloqueada por padrão e pede confirmação explícita do dono antes do deploy — não é sinal de que a mudança ative cobrança de verdade.
- **Domínio próprio `granativa.com.br` — migração completa** (2026-07-08): comprado no registro.br, adicionado no Vercel (Settings → Domains, apex `A` `216.198.79.1` + `www` CNAME), zona DNS configurada no registro.br, código atualizado (canonical/OG em `index.html`, `sitemap.xml`, `robots.txt`, links de push das Cloud Functions em `functions/src/`) e `functions` deployado em produção. HTTPS válido, landing carregando, **login com Google testado e confirmado funcionando** no domínio novo pelo próprio dono. Domínio recém-registrado fica bloqueado pra edição de DNS por algumas horas ("domínio em transição") — se precisar mexer em outro domínio novo no futuro, esperar isso passar antes de tentar. `src/components/Seo.tsx` já era dinâmico via `window.location.origin`, não precisou mudar.
- **`validCoupleWorkspaceUpdate` precisa de uma branch por transição possível** (2026-07-08): a regra em `firestore.rules` valida `workspaces/{id}` por transição explícita (parceiro aceitando 1→2, saindo/removido 2→1). `updateCoupleMode()` (só muda `coupleMode`+`updatedAt`) não batia em nenhuma das duas e sempre negava — "Missing or insufficient permissions" pros dois lados. Terceira branch adicionada pra update-só-de-modo (`activeMemberCount`/`partnerUserId` inalterados) por qualquer `isActiveMember(workspaceId)`. Se um novo tipo de update no workspace do casal for criado, precisa da própria branch aqui — a regra não tem um caso genérico "campo X mudou, resto igual".
- **Se um campo novo entrar num payload de `create`, atualizar o `hasOnly()` da regra correspondente no mesmo commit** (2026-07-09): `createCategory` (`financeService.ts`) ganhou `createdBy` em 17/06, mas `validCategoryCreate` (`firestore.rules`, escrita em 14/06) nunca foi atualizada — toda criação de categoria personalizada foi rejeitada pelo servidor **silenciosamente por ~3 semanas** (o padrão fire-and-forget do app suprime o erro de propósito). O teste de regras (`categoryPayload` em `tests/firestore.rules.test.ts`) também não tinha `createdBy` no payload de teste, então nunca pegou o bug. Ao corrigir, cuidado com o mesmo erro ao contrário: exigir `createdBy` sem condição quebra o seeding de categorias padrão (`ensureDefaultCategories`/`buildDefaultCategory`, que nunca envia `createdBy` — só `isDefault: true`); a regra final trata os dois casos (`isDefault: true` sem `createdBy` OU `isDefault: false` com `createdBy == request.auth.uid`). **Lição geral**: sempre que um campo novo for adicionado a um payload de escrita em `financeService.ts`/`cardService.ts`/`sharedService.ts`/etc., checar no mesmo commit se a regra correspondente em `firestore.rules` permite esse campo — e se o payload do teste de regras reflete o payload real do cliente, não uma versão simplificada.
- Cores só em `src/styles/themes.css` (+ `src/theme/palette.ts`). Literais quebram o teste `noHardcodedColors` (exceção: `src/landing/`).
- Componentes-base de UX: `BottomSheet`, `SelectField`, `CategoryField`, `ConfirmDialog`, `EmptyState`.
- Não expor erro técnico ao usuário; landing sempre clara.
- **Data exibida pro usuário: sempre `formatFriendlyDate`, nunca `toDateInputValue`** (2026-07-09): `toDateInputValue` (`financeDates.ts`) é `yyyy-MM-dd`, feito só pra preencher `<input type="date">` — usá-la como texto de tela mostra data crua tipo "2026-07-08" pro usuário. `formatFriendlyDate` (mesmo arquivo) retorna "Hoje"/"Ontem"/`d MMM`/`d MMM yyyy` com `locale: ptBR` do date-fns (sem o locale, nomes de mês saem em inglês). Já corrigido na Dashboard; **ainda pendente** em `TransactionsPage`, `BillsPage`, `InvoicePage`, `CardDetailPage`, `CardsPage`, `RecurringPage`, `SearchPage` (têm um `spawn_task` sinalizando).
- **`.category-mark` precisa do seletor `span.category-mark`, não só `.category-mark`** (2026-07-09): é um `<span>` dentro de `.list-row`, e a regra genérica `.list-row strong, .list-row span { display: block; }` (empilha texto nas linhas de lista) tem especificidade maior e derrubava o `display: grid` que centraliza o ícone — sintoma: ícone encostado no canto superior-esquerdo do tile colorido, só visível renderizado (não aparece lendo o componente isolado). Se qualquer outro tile/ícone for colocado direto dentro de `.list-row` no futuro, mesmo cuidado de especificidade se aplica. **Segundo caso real (2026-07-12)**: `.service-mark` (selo de serviço em Recorrências/Compromissos) tinha o mesmo bug — iniciais/ícone encostados à esquerda do tile — e virou `span.service-mark` pela mesma razão.
- `EmptyState` tem 6 ilustrações: `transactions`, `cards`, `wallet`, `shared`, `goals`, `bills` (2026-07-09, calendário+check — "nenhum compromisso pendente").
- **Cuidado ao agrupar seletores CSS com vírgula: uma classe morta pode vazar estilo pra uma viva** (2026-07-13): `.entitlement-list li, .notice { display: flex; ... }` em `global.css` — `entitlement-list` é da feature de billing (inativa, não existe em nenhum `.tsx`), mas o `.notice` agrupado na mesma regra é usado em 7 lugares vivos. Qualquer `.notice` com texto misturado a `<strong>` (parágrafo com números em negrito) virava uma fileira de flex-items sem `flex-wrap`, cortando texto e empilhando pedaços fora de ordem — só visível renderizado, não lendo o componente isolado (mesma classe de bug do `.category-mark`/`.service-mark` logo acima, mas por agrupamento em vez de especificidade). `.notice` removido do agrupamento. Antes de agrupar dois seletores com vírgula numa regra, checar se as duas classes são usadas nos mesmos tipos de conteúdo — não só "parece a mesma coisa".
- **`OngoingInstallmentsSheet`/`CardDetailPage` (Fatura atual) — `<strong>` sem `display: block` não empilha com o `<span>` seguinte fora de `.list-row`**: fora das listas (que têm a regra global `.list-row strong, .list-row span { display: block }`), `<strong>valor</strong><span>texto</span>` como irmãos inline sem quebra explícita renderiza colado (`R$ 3.200,002026-07...`, achado pelo dono ao vivo em 2026-07-13). Ao criar um bloco de valor+legenda fora de `.list-row`, sempre por `display: 'block'`/`'grid'` explícito, não assumir que puxa o padrão das listas.

## Cartões e faturas — comportamentos-chave

- **Ledger entries não podem ser apagados/editados por regra** (`firestore.rules`: `ledger/{entryId}` tem `allow update: if false` e `allow delete` restrito a `canDeleteWorkspaceTree`, só usado na exclusão total de conta) — um membro comum nunca consegue reverter um lançamento de fatura diretamente. Por isso, excluir uma transação `card_purchase` (`softDeleteTransaction`, mesmo botão usado em Transações) **não apaga o ledger correspondente** — só marca `deletedAt` na transação. `useCardsData` (2026-07-09) recebe o conjunto de transações excluídas e filtra os ledger entries pelo `sourceTransactionId` antes de calcular saldo/lista da fatura (`calculatedInvoices`), em vez de tentar apagar. Se um novo fluxo precisar reverter um lançamento de fatura, seguir esse padrão de filtro client-side — não adicionar `allow delete`/`allow update` pra membro comum em `ledger` sem decisão explícita (auditoria financeira depende do ledger ser append-only).
- **Cartão excluído é filtrado em `useCardsData`, não nas telas** (2026-07-09): `deleteCard` é soft-delete (`isActive: false`) e `subscribeCards` não filtra. O hook expõe só `activeCards`, só assina faturas de cartão ativo e poda faturas de cartão recém-excluído que já estavam em `state.invoices` (o effect desassina o listener, mas não limpa o que já chegou). Sem isso, o cartão continuava listado em `/app/cards` depois de excluído e as faturas dele seguiam no "Comprometido" e no cálculo de limite. Não voltar a filtrar `isActive` tela a tela.
- **`resolveInstallmentCycle` (`src/cards/cardDates.ts`, 2026-07-09)**: o mês de cada parcela é contado a partir do mês da PRIMEIRA fatura, ancorado no dia 1 — nunca `addMonths(purchaseDate, index)`, que clampa 31/jan em 28/fev e (num cartão que fecha dia 28) fazia a 2ª parcela cair na mesma fatura da 1ª, deixando março vazio. `resolveInvoiceCycle` é o caso `index = 0`.
- **`groupAnticipatablePurchases` (`src/cards/anticipation.ts`, 2026-07-11)**: antecipação é **da última parcela pra trás, contígua** (regra do cartão/Nubank), não parcela avulsa. Agrupa por compra (`sourceTransactionId`), ordena as parcelas futuras da última pra primeira (por `installmentNumber`, fallback `referenceMonth`); a UI oferece "antecipar as últimas [N]" e `installments.slice(0, N)` são essas. Nunca dá pra antecipar a #3 deixando #4/#5. "Fatura futura" é `referenceMonth` MAIOR que a atual; "já antecipado" contado por ocorrência (parcelas irmãs compartilham `sourceTransactionId`). O painel só aparece em fatura **aberta**. O mecanismo (débito na fatura atual + crédito na futura) não muda o total nem o limite — o bug era só a seleção deixar marcar do meio.
- **`invoices/{id}.outstandingBalanceCents` é sempre `0` no Firestore** (2026-07-09) — nasce 0 em `invoicePayload` e nenhum write do cliente atualiza; todos os totais vêm do ledger, calculados no cliente (`useCardsData` + `calculateInvoice`). Qualquer código **server-side** que precise do valor da fatura tem que somar o ledger (ver `invoiceOutstandingCents` em `functions/src/automation.ts`) — ler o campo cru fazia o push de "fatura fechada" anunciar R$ 0,00 sempre.
- **`pickCurrentInvoice` (`src/cards/cardDates.ts`, 2026-07-09)**: usar sempre para achar a fatura "atual" de um cartão (a que acumula compras novas, mais próxima de fechar) — nunca `.find(status === 'open')` direto num array de faturas. Compra parcelada abre fatura `open` em vários meses futuros ao mesmo tempo; `subscribeInvoices` ordena por `referenceMonth desc`, então `.find` pegava a fatura mais distante por engano (`CardDetailPage`/`CardsPage` mostravam "fatura atual" errada e "Pagar agora" pagava a fatura errada).
- **`resolveInvoiceCycle` precisa tratar `dueDay < closingDay`** (`src/cards/cardDates.ts`, 2026-07-09): cartão que fecha tarde no mês e vence cedo no mês seguinte (padrão comum no Brasil: fecha dia 25, vence dia 5) tem o vencimento calculado no mês SEGUINTE ao `referenceDate`, não no mesmo mês — sem isso, o vencimento podia cair antes até da compra que gerou a fatura.
- **Fatura `open` só entra em "Comprometido" (Dashboard) pela data de vencimento real, não pelo mês da fatura** (`buildUpcomingCommitments`, `src/finance/financeCalculations.ts`, 2026-07-09): usa o mesmo `cutoff` de contas a pagar/recorrências (antes do próximo salário registrado, ou 30 dias sem salário futuro). Fatura `closed` continua contando sempre, incondicional. Antes usava `referenceMonth <= mês atual` (mês do CICLO da compra) — em cartão fecha-tarde/vence-mês-seguinte isso contava a fatura inteira como comprometida um mês inteiro antes do vencimento de verdade. Decisão explícita do dono do produto (não mudar sem re-confirmar).
- **`payday` do perfil (`users/{uid}.payday`, 2026-07-09)**: pergunta opcional no onboarding (passo 4/4) + editável em `/app/settings/payday`. `nextPaydayFrom` (`src/finance/payday.ts`) estima a próxima data de recebimento (dia fixo, Xº dia útil sem feriados, ou fim do mês) e vira o 2º fallback do `cutoff` do Comprometido em `calculateDashboardSummary`: `nextIncomeAt` (receita futura já lançada) → `nextPaydayFrom(payday)` → `committedWindowDays` (padrão 30 dias, configurável). Regra do Firestore deployada e verificada ao vivo em produção (na época o emulador local não rodava; desde 2026-07-10 `npm run test:rules` funciona e cobre essa regra).
- **`payday.type === 'variable_income'` é uma escolha real, não ausência de resposta** (2026-07-09): pra quem tem renda variável (plantão, freela, autônomo — não dá pra forçar uma das 3 opções de data fixa). Guardado explicitamente no perfil (não como `payday` ausente) só pra UI de Configurações lembrar a escolha depois de recarregar — pro cálculo, é tratado igual a "sem payday resolvível" (`nextPaydayFrom` nem aceita essa variante no tipo, `ResolvablePaydayRule = Exclude<PaydayRule, {type:'variable_income'}>`). `committedWindowDays` (perfil, padrão 30, editável 1-90 em Configurações por qualquer pessoa) é o que decide o cutoff nesse caso.
- **Dashboard mostra de onde vem o corte do "Comprometido"** (`committedCutoffSource: 'income'|'payday'|'window'|'all'` em `DashboardSummary`, 2026-07-09): legenda embaixo do card explicando "considerando sua receita de X" / "considerando seu recebimento em X" / "considerando os próximos N dias" / "considerando tudo que você já deve". A legenda é um **botão** que reabre o mini tutorial (`.dash-metric-explain`).
- **`availableMode` — a pessoa escolhe como o "Disponível" é calculado** (`users/{uid}.availableMode`). Dois modos: `until_payday` (conta com o próximo recebimento — receita lançada → payday → janela; **default**, `src/finance/availableMode.ts`) e `conservative` (**nunca** assume salário — usa só a janela de dias). **Revisão 2026-07-11**: o conservador *não* conta mais todas as parcelas de uma compra de uma vez (isso jogava o Disponível pra muito negativo, ex.: R$3.000 em 10x = −R$2.000, sem sentido). Agora `resolveCommittedCutoff` no conservador retorna `cutoff = endOfDay(now + committedWindowDays)` e `source = 'window'` — só a parcela que vence dentro da janela pesa. `AvailableModeSheet` (mini tutorial) abre sozinho no primeiro Dashboard de quem não tem o campo; **fechar sem escolher grava o default**. Trocável em `/app/settings/payday`. No conservador a **janela de dias vale** (não é mais só reserva); a **data de recebimento** é que fica inativa nesse modo (recuo `.settings-section--inactive`).
- **`buildUpcomingCommitments` aceita `cutoff: Date | null`** — `null` só se alguém passar explicitamente (nenhum modo faz mais isso; conservador virou janela). `committedCutoff` no `DashboardSummary` é sempre um `Date` na prática.
- **Compra parcelada em andamento** (`registerOngoingInstallments`, `cardService.ts`, 2026-07-11): pra migrar pro app uma compra que já vinha sendo paga (óculos 7/10). Cria só as parcelas que faltam a partir do mês calculado (`planOngoingInstallments`, puro/testável; data da fatura via `invoiceDueDateForReferenceMonth`). UI em `OngoingInstallmentsSheet`, botão na `CardDetailPage`. **Revisão 2026-07-13**: o formulário pedia "próxima parcela é a Nº de M" + "em qual mês cai" (data que a pessoa raramente sabe de cabeça, exigia olhar o extrato — feedback direto do dono usando a própria tela). Trocado por "quando você comprou" (date picker, igual o de compra nova) + "quantas já pagou" — o mês da próxima parcela agora é **calculado** com `resolveInstallmentCycle` (mesma função usada pra compra nova, com o fechamento/vencimento já cadastrados do cartão) e só aparece no resumo pra conferir, não é mais pedido como input cru. `OngoingInstallmentsSheet` passou a receber o `card` inteiro (não só `cardId`) pra ter `closingDay`/`dueDay` disponíveis no cálculo do preview.
- **Rótulo "parcela X/N" na fatura**: `InvoiceLedgerEntry.installmentNumber`/`installmentTotal` (opcionais), preenchidos por `createCardPurchase` (quando parcelado) e `registerOngoingInstallments`. Regra `validInvoiceLedgerCreate` aceita os dois (int 1..72). `ledgerPayload` só inclui as chaves quando há parcela (a regra é `hasOnly`).
- **`resolveCommittedCutoff` (`financeCalculations.ts`) é a fonte única do corte** (2026-07-10): `calculateDashboardSummary` e a tela de Configurações › Recebimento usam a MESMA função, pra que a explicação ("Hoje o corte é 5 ago") nunca divirja do número do Dashboard.
- **`npm run test:rules` voltou a funcionar** (2026-07-10) via `scripts/with-java.mjs` — rodar sempre que tocar `firestore.rules`. Ver `CLAUDE.md` pro diagnóstico do Java quebrado desta máquina.
- **Todo snapshot do Firestore é lido por `readSnapshotDoc`** (`src/firebase/snapshotData.ts`, 2026-07-11), que passa `serverTimestamps: 'estimate'`. Sem isso, `snapshot.data()` devolve `null` pra qualquer `serverTimestamp()` pendente — e como `softDeleteTransaction` grava `deletedAt: serverTimestamp()` (a regra exige `== request.time`), **excluir uma transação offline não fazia nada** até o ack do servidor: ela seguia no Extrato e a compra seguia somando na fatura. Nunca voltar a usar `snapshot.data()` cru num listener.
- **`useCardsData` não confia na janela de `subscribeTransactions`** (2026-07-11): recebe `{ knownIds, deletedIds }` e resolve com `fetchDeletedTransactionIds` (leitura por documento) os `sourceTransactionId` que a janela de 300 não cobre. Na dúvida (erro, doc ausente), **mantém** o lançamento — sumir com ele apagaria dívida real da fatura.
- **Catálogo de serviços/assinaturas** (`src/finance/subscriptionServices.ts`, 2026-07-11): ~60 marcas (streaming, música, jogos, delivery, academia, nuvem, telecom, contas fixas) com nome canônico, apelidos, iniciais, categoria sugerida e `logoPath`. `searchSubscriptionServices` (autocomplete, frouxo, ranqueado por prefixo) alimenta as sugestões nos formulários de Recorrências e Compromissos; `findSubscriptionService` (reconhecimento estrito, casa **palavra inteira** pra alias curto tipo "oi"/"tim" não dar falso positivo) escolhe a marca a exibir na lista. `ServiceMark` (`src/components/ServiceMark.tsx`) tem **3 estados** (2026-07-12): `logoPath` (SVG oficial, chip `--brand-chip-bg` claro pra logo preto não sumir no dark); **tile "ícone de app"** na cor da marca com iniciais brancas quando a marca só tem wordmark (`serviceBrandColors` em `theme/palette.ts`, estado `.service-mark--brand` — mantém a borda sutil pros tiles escuros não sumirem no dark); e tile de iniciais neutro pros genéricos (Aluguel, Água…). `logoPath` tem prioridade sobre a cor de marca. SVGs em `public/service-logos/`: os gerados por `npm run generate:service-logos` (simple-icons, `SOURCES.md` automático) + 6 trazidos à mão pelo dono (ChatGPT, Microsoft 365, Oi, Google One, Claro, Rappi — procedência em `MANUAL_SOURCES.md`, que o script não conhece).
- **Recorrência é idempotente por ocorrência** (2026-07-11): `recurringOccurrenceTransactionId(ruleId, occurrenceAt)` (em `financeService.ts`, espelhado em `functions/src/automation.ts`) dá à Cloud Function das 6h e ao botão "Registrar" o mesmo id de documento. A segunda escrita é um payload de create sobre um doc existente e a regra a rejeita, desfazendo o batch inteiro. A data sai em **UTC**, senão BRT (app) e UTC (função) gerariam chaves diferentes. A UI ainda esconde "Registrar" quando `isRecurrenceDue` é falso.
- **O corte do Comprometido é um DIA, não um instante** (2026-07-09): `endOfDay(cutoff)` em `calculateDashboardSummary`. As três origens produziam horas diferentes (vencimentos ao meio-dia via `fromDateInputValue`; `nextPaydayFrom` à meia-noite; `addDays(now, N)` na hora atual) — sem normalizar, uma conta que vencia no próprio dia do salário sumia do Comprometido, e a janela de N dias dava resultados diferentes de manhã e à noite. `findNextIncomeDate` também ignora receita datada de hoje (já está no saldo) em vez de comparar com o instante `now`.
- Transações `card_purchase` não são editáveis pelo formulário genérico (`EditTransactionPage`) — não tem noção de cartão/parcela e corromperia o registro sem sincronizar o ledger. Botão "Editar" escondido pra esse tipo; única correção é excluir e lançar de novo.
- **Fatura aberta** permanece `open` até o fechamento automático pela `closeInvoicesDue` (meia-noite do dia de fechamento). Qualquer pagamento em fatura aberta vira `advance_payment`. Os botões "Fechar fatura" e "Conciliar manualmente" foram removidos da `InvoicePage` — a automação cuida disso.
- **⚠️ Antecipação de parcelas ficou quebrada em produção desde a criação da feature até 2026-07-09** — bug real, não hipotético: `validInvoiceLedgerEntryType` (`firestore.rules`) nunca aceitou o tipo `'installment_anticipation_credit'` (só o débito `installment_anticipation` estava na lista, o crédito não). Resultado: **todo clique em "Confirmar antecipação" (`InvoicePage`) era rejeitado pelo servidor, silenciosamente, desde sempre** — a UI parecia funcionar (fire-and-forget), mas nada persistia; recarregar a página desfazia tudo. Corrigido (`installment_anticipation_credit` adicionado à lista) e implantado; confirmado ao vivo em produção (fatura atual +valor, fatura futura reduzida, limite total inalterado). Este é o **2º incidente** do mesmo padrão que o bug de `createdBy`/categoria (2026-06) — ver regra permanente **"⚠️ REGRA PRINCIPAL: todo valor novo de enum num payload do Firestore precisa atualizar a regra no MESMO commit"** em `CLAUDE.md`, criada por causa desses dois incidentes.
- **Débito de antecipação é um lançamento por parcela, não um único somado** (`anticipateInstallments`, `src/cards/cardService.ts`, 2026-07-09): cada débito carrega o `sourceTransactionId` da compra original correspondente, igual o crédito. Um débito somado sem esse vínculo nunca seria limpo pelo filtro de ledger órfão (`useCardsData.ts`) se a compra original fosse excluída depois de antecipada.
- **Antecipação de parcelas** (`InvoicePage`, 2026-07-11): painel agrupa por compra e oferece um seletor "antecipar as últimas [N] parcelas" (ver `groupAnticipatablePurchases`). Ao confirmar: `writeBatch` adiciona `installment_anticipation_credit` em cada fatura futura escolhida (reduz saldo delas) e `installment_anticipation` na fatura atual (um lançamento por parcela, com o `sourceTransactionId` da compra). Fire-and-forget.
- **A lista "Compras" da fatura precisa incluir `installment_anticipation`, não só `purchase`** (2026-07-12): o total do topo (`purchasesTotalCents`, via `calculateInvoice`) soma os dois tipos, mas a lista abaixo dele filtrava só `purchase` — uma parcela antecipada pra dentro da fatura atual inflava o total sem aparecer em nenhuma linha (achado ao vivo: total "R$1.200" com a lista mostrando só "R$300"). `purchases` em `InvoicePage.tsx` agora inclui os dois tipos; entrada antecipada é rotulada "Parcela antecipada" (não tem `installmentNumber`/`installmentTotal`, então não dá pra mostrar "X/N" nela).
- **Parcela antecipada some da fatura FUTURA, igual Nubank** (2026-07-12): `anticipatedAwayEntryIds` (`src/cards/anticipation.ts`) casa cada `purchase` com o `installment_anticipation_credit` que a anula (mesma compra, mesmo valor — via `sourceTransactionId` + `amountCents`) e esconde os dois. `InvoicePage` deixa de listar essa parcela na fatura de destino e some com a linha "Compras" do resumo quando não sobra nada; `CardDetailPage` some com a fatura do "Histórico de faturas" via `invoiceHasVisibleActivity` quando, depois de esconder o par, não resta nenhuma atividade real. **Puramente presentational** — o ledger e `calculateInvoice`/`useCardsData` continuam intocados; se uma compra nova cair nessa mesma fatura depois, ela deixa de estar "vazia" e reaparece sozinha, sem estado gravado pra migrar.
- **`installmentNumber`/`installmentTotal` agora trafegam na antecipação** (2026-07-12): antes eram descartados entre `InvoicePage` e `anticipateInstallments` — nem o débito (fatura de origem) nem o crédito guardavam qual parcela era antecipada, então a origem só dizia "Parcela antecipada" genérico. Combinado com o fix acima (parcela some da fatura futura), a última parcela visível de uma compra (ex.: "7/10") parecia incompleta, sem indicar que 8/9/10 foram antecipadas. `anticipateInstallmentsSchema` ganhou os dois campos opcionais por parcela; a regra do Firestore **já aceitava** genericamente (nenhuma mudança de regra). Origem agora rotula "parcela 8/10 antecipada"; antecipações antigas sem o dado gravado caem no fallback genérico.
- **Trazer compras existentes ao criar o cartão** (2026-07-11): `createCreditCard` devolve o id e a `CardsPage` navega pra `/app/cards/{id}?novo=1`. A `CardDetailPage` mostra um destaque (`.card-onboarding-callout`) quando `?novo=1` ou o cartão não tem compras, abrindo o `OngoingInstallmentsSheet` (que cobre parcelas em andamento E compras futuras que começam mais pra frente).
- **Faturas carregadas com limite** (2026-07-07): `subscribeInvoices` traz só as 24 mais recentes por cartão (~2 anos). Sem isso, cada fatura abria seu próprio listener de ledger em `useCardsData` e o total de listeners crescia sem parar conforme a conta envelhecia.
- **Status `overdue` desde 2026-07-12**: fatura fechada, com saldo devedor e vencimento no passado agora resolve pra `'overdue'` (`resolveInvoiceStatus`, `calculateInvoice.ts`, recebe `dueDate` opcional). Também corrigido no mesmo commit: `useCardsData` recalculava o status só pra fatura `open`/`closed` — uma fatura já reconciliada manualmente (`paid`/`partial`/`overpaid`) ficava com o status congelado pra sempre mesmo com ledger novo (ex.: reembolso chegando depois). `lifecycle` agora é `'closed'` pra qualquer status que não seja `'open'`, e o status exibido é sempre o `calculation.status` recém-calculado. Contas a pagar (`Bill`) têm o mesmo comportamento desde a mesma data via `markOverdueBills` (`financeService.ts`), rodando a cada snapshot de `subscribeBills`.

## Recorrências — comportamento-chave

- **`anchorDay`** (2026-07-07): `RecurringRule` guarda o dia do mês (1-31) pretendido na criação. `nextOccurrenceDate` (`src/finance/financeService.ts` e a cópia server-side em `functions/src/automation.ts`) usa esse dia fixo — não o da última ocorrência — pra calcular a próxima, clampando no último dia válido do mês alvo. Uma recorrência no dia 31 clampa em 28/fev, mas **volta pro dia 31** assim que março (31 dias) chega, em vez de ficar presa no dia clampado pra sempre. Mesmo comportamento para recorrência anual em 29/fev. Regras antigas (criadas antes deste campo existir) não têm `anchorDay` e mantêm o comportamento de clamp simples — sem migração retroativa.
- **Pagar adiantado** (`canRegisterRecurrence`, `RECURRENCE_EARLY_PAY_DAYS = 7`, 2026-07-12): `RecurringPage` mostra "Registrar" quando a ocorrência venceu, "Pagar adiantado" quando falta ≤ 7 dias, e "Em dia" além disso. Liberar adiantado é seguro porque a transação da ocorrência tem id pela **data de vencimento** (`recurringOccurrenceTransactionId`), não pela data do pagamento — registrar hoje e a automação das 6h rodar no vencimento caem no mesmo doc, sem duplicar. Registrar avança `nextOccurrenceAt` pro próximo período (a transação fica datada de hoje).

## Análise (`/app/search`) — comportamento-chave

- **Gasto em regime de caixa, por parcela** (`src/finance/spendingAnalysis.ts`, 2026-07-11): a Análise conta o cartão pela **parcela que cai na fatura de cada mês**, não pela transação `card_purchase` (que guarda o valor cheio no mês da compra). Uma compra de R$3.000 em 10x vira R$300 × 10 meses, não R$3.000 num mês só. Reusa a soma de sinais do ledger (`recognizedExpenseCents` = purchases+fees−credits, com `installment_anticipation` no débito e `installment_anticipation_credit` no crédito), então **antecipar parcela move o gasto do mês futuro pro atual de graça** aqui também. Módulo puro/testável (`spendingAnalysis.test.ts`, 11 casos); a categoria de cada parcela vem da transação-mãe via `sourceTransactionId`. `SearchPage` só monta a UI. Seção "Compras parceladas — Em andamento" (`ongoingInstallmentPurchases`) dá visibilidade ao valor cheio que a visão por parcela dilui; "restante" é líquido de antecipação. Busca mostra "Nx de R$Y" num resultado de compra no cartão.
- **`MonthSwitcher` (2026-07-08; futuro em 2026-07-12)**: navegação por mês (`‹ Mês de Ano ›`, `src/pages/SearchPage.tsx`). `selectedMonth` (`'YYYY-MM'`) controla KPI strip, doughnut por categoria e "vs. mês anterior"; sem limite pra trás. `ChevronRight` agora **avança pra meses futuros até `lastCommittedMonth`** (último mês com parcela/conta comprometida) — antes travava no mês atual. O gráfico de histórico (barras, últimos 6 meses) fica fixo como tendência e **não** acompanha `selectedMonth`.
- **Mês futuro = "Previsto", não "Gasto"** (2026-07-12): num mês que ainda não chegou não há gasto realizado, então a Análise mostra a previsão — **comprometido** (parcelas de cartão caindo na fatura + contas a pagar vencendo: `committedByCategoryForMonth`/`billsByCategoryForMonth`) **+ recorrências projetadas** (estimativa: `projectedRecurringForMonth`/`recurringByCategoryForMonth`, com `nextOccurrenceDate` injetado pra manter o módulo puro). KPI "Previsto no mês" (total), terceiro card vira "Recorrências ~R$", seção "Recorrências previstas" lista as regras, legenda distingue comprometido (firme) de recorrência (estimativa). "vs. mês anterior" some. O horizonte de navegação (`maxMonth`) vai até a última parcela/conta **ou** +12 meses se houver recorrência ativa (recorrência é "infinita", precisa de teto).
- **`.metric-card`/`.metric-icon` (`global.css`)** finalmente em uso — existiam prontos e sem nenhum `.tsx` usando antes. Modificador `.metric-card-value--compact` (`-webkit-line-clamp: 2`) é pra valores de texto longo (nome de categoria), não pra dinheiro/porcentagem — sem ele a tipografia grande de número corta nome de categoria tipo "Alimentação" em "Alimenta...".
- **Busca por texto é `BottomSheet` sob demanda** na Análise (busca global: transações, contas, compromissos) — ícone de lupa no cabeçalho. O link "Buscar" do Dashboard (`DashboardPage.tsx`, card "Resumo de gastos") navega com `state={{ autoOpenSearch: true }}` pra já abrir a sheet. **A tela de Transações (`TransactionsPage.tsx`) tem busca inline própria** (2026-07-12): barra sempre visível + chips de tipo (Tudo/Despesas/Receitas/Transferências) + **seletor "Cartão"** (ver só as compras de um cartão), filtrando ao vivo por nome/categoria/tag/estabelecimento (`.transactions-filter`). As linhas de "Próximos compromissos" no Dashboard são **clicáveis** (fatura → fatura do cartão; conta → Compromissos; recorrência → Recorrências) via `UpcomingCommitment.cardId` + `.list-row--link`.

## Firestore (coleções por workspace)

`workspaces/{id}/` → `accounts`, `categories`, `transactions`, `bills`, `recurring`, `goals`, `goalContributions`, `cards/{cardId}/invoices/{invoiceId}/ledger`, `members`, `sharedExpenseClaims`, `settlements`, `invites`. Workspace pessoal = `personal_{uid}`; workspace do casal é separado, com membership ativa.

## Funcionalidades-chave do casal

- **Página dividida em `src/pages/shared/`** (2026-07-08): `SharedSpacePage.tsx` virou orquestrador dos 3 estados (sem espaço / aguardando parceiro / com parceiro); `CoupleInviteSection.tsx`, `CoupleModeSheet.tsx`, `CoupleSavingsSection.tsx` e `CoupleExpensesSection.tsx` concentram cada pedaço. Reestruturação total pedida pelo dono (UX de convite estava confusa — até 6 botões visíveis ao mesmo tempo).
- **Convite com um estado por vez** (2026-07-08): o cartão de convite mostra só a ação relevante pro momento (gerar / compartilhar recém-gerado / convite ativo pós-reload), não os 6 botões de antes. `generatedInvite` (código bruto + QR) só existe em `useState` local — depois de um reload, a UI usa o `activeInvite` do Firestore pra mostrar "Convite ativo, expira em..." em vez de reoferecer "Gerar convite" (que antes invalidava silenciosamente o código já enviado ao parceiro). "Compartilhar convite" tenta `navigator.share` antes de cair pro clipboard. `cleanupExpiredInvites` roda sozinho ao montar a seção — sem botão manual (o TTL nativo do Firestore já cobre o resto).
- **Modos do espaço** (`coupleMode` no workspace): **níveis progressivos, não toggles independentes** — é sempre UM valor só. `savings_only` (só cofrinho, padrão) → `transparent` (cofrinho + despesas divididas visíveis, sem acerto formal) → `balanced` (idem + barra proporcional de quem cobre mais no mês). Cada nível superior mostra as seções do anterior + a sua; por isso "subir" de modo faz aparecer mais UI (isso confundiu o dono — parecia "ativar os 3 juntos"). Mitigado (2026-07-08) com: badge visível do modo atual no topo do espaço parceirado (`.couple-mode-badge`, clicável → abre o seletor), texto explicando que o cofrinho funciona em qualquer modo, botões renomeados "Ativar X" → "Mudar pra X", e tag "Atual" no seletor marcando o modo vigente (distinto do que está tentativamente selecionado). Pode mudar a qualquer momento pelo badge ou em "Gerenciar espaço".
- **Divisão de despesa** (claims, apenas nos modos transparent/balanced): igual / porcentagem / valor. Sem acerto de contas formal — o equilíbrio é visual/proporcional.
- **Cofrinho do casal**: meta compartilhada (`goals` no workspace do casal) + contribuições (`goalContributions`, campo `type: 'deposit' | 'withdrawal'`). "Guardar" pode descontar de uma conta pessoal (vira despesa "Cofrinho" no workspace pessoal); "Resgatar" (2026-07-07) faz o inverso — desconta do total do casal e pode creditar como entrada numa conta pessoal. Categoria fixa `both_cofrinho` em ambos. Total exibido sempre vem de `goal.savedCents` (fonte da verdade); `goalContributions` só alimenta a quebra por pessoa/mês (`calculateCoupleGoalStats`, `src/domain/shared/`). Resgate valida client-side (não pode exceder o total) e é bloqueado server-side pela regra (`savedCents` nunca fica negativo).
- **Guardar/resgatar é atômico entre os 2 workspaces desde 2026-07-12** (`coupleGoalDeposit`/`coupleGoalWithdraw`, `financeService.ts`): um único `writeBatch` grava a contribuição + incrementa `goal.savedCents` (workspace do casal) **e** cria a transação pessoal correspondente (workspace pessoal) — Firestore permite um batch tocar documentos de workspaces diferentes, as regras avaliam por documento, não por batch. Antes eram 2 writes independentes (`addGoalContribution`+`createTransaction`), e a transação pessoal tinha `.catch(() => undefined)` — se ela falhasse sozinha, o cofrinho subia sem o dinheiro sair de lugar nenhum, sem nenhum aviso. As funções antigas (`addGoalContribution`/`withdrawGoalContribution`) foram removidas (sem caller). Metas **pessoais** usam uma função à parte, `contributeToGoalWithTransaction` (já era atômica, single-workspace, sem essa história).

## Admin (`/admin`) — comportamento-chave

- **Único admin**: `a.thurcos@gmail.com`, hardcoded em `src/auth/routeGuards.tsx` (`RequireAdmin`), `functions-admin/src/index.ts` (`ADMIN_EMAIL`) e `firestore.rules` (`isAdmin()`). Precisa mudar nos 3 lugares se um dia virar multi-admin.
- **Confirmação de ações destrutivas**: exclusão de conta exige digitar `EXCLUIR` (frase fixa — nunca comparar com um campo de usuário como nome, que pode estar vazio). Auto-exclusão bloqueada na UI (linha da própria conta não mostra botão de deletar).
- **Convites são revogáveis por admin** (2026-07-07): regra do Firestore para `coupleInvites` inclui `isAdmin()` no `allow delete`. Reusa `revokeCoupleInvite` de `sharedService.ts` — não duplicar lógica de revogação no admin.
- **Paginação por cursor** (2026-07-08): `getAdminUsers`/`getAdminCoupleWorkspaces`/`getAdminInvites` (`src/admin/adminService.ts`) não têm mais teto fixo — recebem um cursor opcional (`AdminCursor`, doc snapshot) e devolvem `{ items, cursor, hasMore }` em páginas de `ADMIN_PAGE_SIZE` (100). `AdminPage.tsx` mantém um botão "Carregar mais" por aba. `formatCount(count, hasMore)` (`src/admin/adminFormat.ts`) mostra `"N+"` quando ainda há mais página pra carregar — não é mais contra um teto numérico.
- **Painel de detalhes do usuário** (2026-07-08): clicar na linha da tabela de Usuários abre `UserDetailModal` — mostra perfil e a lista de espaços do usuário (`getAdminUserWorkspaceRefs` + `getAdminWorkspacesByIds`, ambas em `adminService.ts`). Só usa dados que o admin já lê hoje (`users/{uid}` e `workspaces/{id}` têm `isAdmin()` nas regras); **não** expõe transações/contas/faturas — essas coleções exigem `isActiveMember(workspaceId)` de propósito (`docs/PRIVACY.md`), não abrir essa regra sem decisão explícita.
- **Forçar logout** (2026-07-08): nova Cloud Function `adminForceLogout` (`functions-admin/src/index.ts`, mesmo padrão de `adminDeleteUser`) chama `auth.revokeRefreshTokens(userId)`. Invalida sessão só na próxima renovação de token (não é instantâneo — token atual continua válido ~1h). Precisa `npx firebase deploy --only functions --project zerou-26757` antes de funcionar em produção.
- **Exclusão 100%** (2026-07-08): achei e corrigi um vazamento real — `users/{uid}/fcmTokens` (token de push, `src/pwa/notifications.ts`) nunca era apagado nem na autoexclusão nem na exclusão pelo admin. Corrigido nos dois fluxos (`src/settings/accountDeletionService.ts` e `functions-admin/src/index.ts`); as duas listas de subcoleções de workspace (`WORKSPACE_COLLECTIONS`) foram alinhadas (`comments` incluída nos dois).
- **IAM do Cloud Run pode se perder quando uma callable é recriada** (2026-07-09): `adminDeleteUser` ficou sem `roles/run.invoker` pra `allUsers` no serviço Cloud Run por baixo — provavelmente perdido no redeploy que resolveu o conflito de codebases (2026-07-07, mover function entre codebases recria o recurso). Sintoma: cliente via erro genérico `"internal"` em vez do erro de aplicação esperado; `curl` direto sem auth confirmava — resposta HTML do Google Frontend (bloqueio de infra, nunca chega no código) em vez do JSON `PERMISSION_DENIED` que `adminForceLogout` retorna. `firebase deploy` de **update** numa function existente não reaplica esse binding sozinho. Fix: `setIamPolicy` direto na API do Cloud Run (`run.googleapis.com/v2/.../services/{nome}:setIamPolicy`, `{"bindings":[{"role":"roles/run.invoker","members":["allUsers"]}]}`) — dá pra autenticar com o access token já em cache do `firebase-tools` (`~/.config/configstore/firebase-tools.json`), sem precisar de `gcloud` instalado. Se qualquer callable for recriada de novo no futuro, checar a IAM policy depois em vez de assumir que o deploy cuidou disso.

## Deploy de regras

`npx firebase deploy --only firestore:rules --project zerou-26757` (só regras de segurança; não toca billing/functions/hosting).

## Fim de sessão

- `CHANGELOG.md`: resumo curto quando houver entrega relevante.
- Este `SESSAO.md`: só quando mudar estado atual, stack, fluxo, caminhos ou regra essencial. Não é diário.
- `docs/history/YYYY-MM.md`: detalhes que não cabem no changelog.
- `docs/planning/TODOS.md`: pendências.
- Regra completa de decisão em `CLAUDE.md`.
